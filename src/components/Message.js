// function Message(props) {
//   return <p className="message">{props.message}</p>;
// }

// export default Message;

const Message = (props) => {
  return <p className="message">{props.message}</p>;
};

export default Message;
