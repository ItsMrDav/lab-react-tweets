// function ProfileImage(props) {
//   return <img src={props.image} className="profile" alt="profile" />;
// }

// export default ProfileImage;

const ProfileImage = (props) => {
  return <img src={props.image} alt="profile pic" className="profile" />;
};

export default ProfileImage;
